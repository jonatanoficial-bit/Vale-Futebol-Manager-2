import { screenWrap, topbar } from './common.js';
import { teams } from '../data/gameData.js';
import { safeImg, clubLogo, country } from '../systems/assets.js';
import { money } from '../utils/dom.js';
export function confirmCareer(state){ const t=teams.find(x=>x.id===state.clubId)||teams[0]; return screenWrap('confirmCareer', `${topbar('Confirme seu desafio','Revise suas escolhas','teamSelect')}<section class="panel stack"><div class="row">${safeImg(state.manager.avatar,'avatar','Manager','manager-pic')}<div><div class="small">Nome do manager</div><h2>${state.manager.name}</h2><div class="row">${safeImg(country(state.manager.country),'country','Brasil','')} Brasil</div></div></div><hr><div class="row space"><div>${safeImg(clubLogo(t.id),'club',t.name,'club-logo')}<h2>${t.name}</h2><p class="small">${t.countryName} · Fundação: 1912</p></div><div class="panel"><div class="small">Valor do time</div><h2>${money(t.value)}</h2><div class="tag">${t.level} Geral</div></div></div><div><h3>Competições</h3><div class="grid grid-2">${t.competitions.map(c=>`<div class="card">🏆 ${c}</div>`).join('')}</div></div><button class="main-btn" data-route="lobby">Confirmar e começar</button></section>`, true); }
