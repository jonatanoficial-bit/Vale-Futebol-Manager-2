# Upload pelo Git Bash

Para publicar esta versão depois de extrair/substituir os arquivos no repositório:

```bash
cd "/c/Users/jonat/Desktop/GAME/¨2026/FUTEBOL V2"

git status
git add .
git commit -m "World Edition 11.0.0"
git push -u origin main
```

O commit e o push não foram executados automaticamente.
